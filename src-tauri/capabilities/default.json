{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main","translator"],
  "permissions": [
    "core:default",
    "opener:default",
    "clipboard-manager:default"
  ]
}
